# Server
PORT=3001
NODE_ENV=production
FRONTEND_URL=http://localhost:3000

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=pativar
DB_USER=pativar
DB_PASSWORD=pativar123

# JWT
JWT_SECRET=change-this-to-a-very-secure-random-string-in-production

# API
API_BASE_URL=http://localhost:3001

# Firebase Admin (service account — Firebase Console > Project Settings > Service accounts > Generate new private key)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
